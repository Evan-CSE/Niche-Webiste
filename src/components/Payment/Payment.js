import React from 'react'
import Footer from '../Footer/Footer'
import MenuBar from '../MenuBar/MenuBar'

export default function Payment() {
    return (
        <div>
            <MenuBar></MenuBar>
            <h1 className='text-center text-6xl text-red-700'>
                Payment System Coming soon. Until then you can pay with bKash going to order now menu inside each product card.
            </h1>
            <Footer></Footer>
        </div>
    )
}
